'use strict'

const config = {
  db: {
    host: 'localhost',
    port: 28015,
    db: 'mydb_platzi_desde_platzi_2',
    setup: true
  }
}

// secret: env.process.PLATZIGRAM_SECRET

module.exports = config
